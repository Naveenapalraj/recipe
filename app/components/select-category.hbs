<select {{on 'change' selectedCategory value='target.value'}}>
  {{#each this.categoryValue.categoryValue as |category|}}
    <option value ={{category.id }} selected={{eq category.id  @categoryData.id}}>{{category.categoryName}}</option>
  {{/each}}
</select>